from typing import Dict
from collections import defaultdict


# TODO: recieve an element on the page from BS4 (see extract_fields output)
def tokenize(Dict[str, str]) -> Dict[str, int]:
    return defaultdict()
